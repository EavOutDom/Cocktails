import React from "react";

const Error = () => {
    return (
        <div className="my-12 text-center font-semibold text-xl">
            404. That's an error.
        </div>
    );
};

export default Error;
